<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="css/bootstrap.min.css">
    <title>Library</title>
</head>
<body class="container">
    <nav class="navbar navbar-expand-sm navbar-dark bg-dark">
        <a class="navbar-brand" href="/">MyBook</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/books">Books</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link disabled" href="/borrows">Borrows</a>
                </li>
            </ul>
        </div>
    </nav>
    <div class="container">
        <div class="row">
            <div class="col-9">
                <div class="row">
                    {{#unless borrows}}
                    <div class="col-9 mt-3 ml-3">
                        <h5>No Books Available!</h5>
                    </div>
                    {{/unless}}
                    {{#each borrows}}
                    <div class="col-11 offset-1 mt-3">
                        <div class="card">
                            <div class="card-header d-flex">
                                <span class="ml-auto badge badge-pill badge-{{borrowClass this.bookStatus}}">{{borrowStatus this.bookStatus}}</span>
                            </div>
                            <div class="card-body">
                                <h4 class="card-title">{{this.bookName}} <small class="text-muted"> | {{this.borName}}</small></h4>
                                <p class="card-subtitle mt-4 mb-3 text-muted">Loan date &nbsp; &nbsp;<span class="badge badge-pill badge-light">{{this.borrowAt}}</span> </p>
                                {{#if this.bookStatus}}
                                <a class="card-link" href="/retreive/{{this.bookID}}">retrieved</a>
                                {{/if}}
                            </div>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>
            <div class="col-3 mt-3">
                <nav class="nav flex-column">
                    <a class="nav-link active" href="/borrows">Borrows</a>
                    <a class="nav-link" href="/newborrow">New Borrow</a>
                    <a class="nav-link active" href="/borrowers">Borrowers</a>
                    <a class="nav-link" href="/newborrower">New Borrower</a>
                </nav>
            </div>
        </div>
    </div>
</body>
</html>